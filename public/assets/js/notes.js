const notes = require('express').Router();
//create function to randomize id

//get route for retrieving all of the notes
//notes.get('/, (req,res) => {
    //location of data from file => res.json(JSON.parse(data));
// });


//post route for a submitted note
//notes.post('/, (req,res) => {
    //const{}= req.body;
    //req.boy
    //const = newNote
    //function to make random ID

//function to read the data, parse the data, stringify, and write to file
//Console log message : status of whether or not note was logged

module.exports = notes;